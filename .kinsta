# Kinsta Application Hosting Configuration

# Use Docker for build
build_method = "docker"

# Environment variables
env_vars = [
    "SECRET_KEY",
    "DATABASE_URL", 
    "DEBUG",
    "UBI_DATABASE_URL",
    "DB_CA_CERT",
    "KINSTA_DOMAIN"
]